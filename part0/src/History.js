import React from 'react';


const History = (props) => {
        return (
            <div>
               <p>{props.text} {props.result}</p>
            </div>
        )
    }

export default History;
